apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-product
  namespace: ecommerce   # ✅ fixed
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-product
  template:
    metadata:
      labels:
        app: mongo-product
    spec:
      containers:
        - name: mongo
          image: mongo
          ports:
            - containerPort: 27017
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
      volumes:
        - name: mongo-storage
          emptyDir: {}   # ⚠️ temp storage (ok for demo)
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-product
  namespace: ecommerce   # ✅ fixed
spec:
  selector:
    app: mongo-product
  ports:
    - port: 27017
      targetPort: 27017
  type: ClusterIP